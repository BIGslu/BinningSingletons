% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_BinningSingletons.R
\name{theme_BinningSingletons}
\alias{theme_BinningSingletons}
\title{Apply theme (similar to theme_classic) and add Binning Singletons logo to ggplot}
\usage{
theme_BinningSingletons(base_size = 11, logo_x = 1, logo_y = 1)
}
\arguments{
\item{base_size}{Numeric base font size. Default is 11}

\item{logo_x}{Numeric x-axis start location for logo. Used in rasterGrob( ) as normalised parent coordinates (npc)}

\item{logo_y}{Numeric y-axis start location for logo. Used in rasterGrob( ) as normalised parent coordinates (npc)}
}
\description{
Apply theme (similar to theme_classic) and add Binning Singletons logo to ggplot
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(x=mpg,y=wt)) +
    geom_point(aes(color=as.character(cyl)), size=3)  +
    theme_BinningSingletons(logo_x = 0.5, logo_y = 0.1)

ggplot(mtcars, aes(x=disp,y=drat)) +
    geom_point(aes(color=as.character(cyl)), size=3) +
    scale_color_BinningSingletons(type = "discrete", aesthetic = "color") +
    theme_BinningSingletons()
}
